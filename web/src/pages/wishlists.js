import React from 'react';

function Wishlists({title, data}) {
    console.log(data);
    console.log(title);
    
    return (
        <span>wishlists here</span>
    )
};

export default Wishlists;